<!-- source: README.md blob 60ae625d4043 | translated: 2026-09-06 | reviewed: - -->
# NarrativeTrace

[English](README.md) | [Español](LEAME.md) | [Português](LEIAME.md) | **简体中文**

> 代码即日志。

NarrativeTrace™ 把运行中的 Java 代码变成一份可读的执行叙事,它由你早已写好的
方法名、类名和参数名构建而成。不需要 `logger.info(...)` 这样的行。如果追踪难
以阅读,需要的是重构代码,而不是更多日志语句。

赶时间:[运行演示](#一条命令先试试) → [加到一个测试里](#加到一个测试里) →
[选择你的集成方式](#选择你的集成方式)。

## 问题所在

这个方法有一半是日志噪音:

```java
public OrderResult placeOrder(String customerId, String productId, int quantity) {
    logger.info("Placing order for customer {} product {} quantity {}", customerId, productId, quantity);

    var inventory = inventoryService.reserve(productId, quantity);
    logger.debug("Reserved inventory: {}", inventory);

    var payment = paymentService.charge(customerId, inventory.total());
    logger.info("Payment processed: {}", payment.transactionId());

    var result = new OrderResult(payment.transactionId(), inventory.items());
    logger.info("Order placed successfully: {}", result);
    return result;
}
```

业务逻辑只有三行,日志却有四行。每个开发者写日志的方式都不一样 —— 消息不同、
级别不同、包含的值也不同。结果就是不一致、冗长,而且与它所描述的代码纠缠在
一起。

NarrativeTrace 把这些统统去掉:

```java
public OrderResult placeOrder(String customerId, String productId, int quantity) {
    var inventory = inventoryService.reserve(productId, quantity);
    var payment = paymentService.charge(customerId, inventory.total());
    return new OrderResult(payment.transactionId(), inventory.items());
}
```

纯粹的业务逻辑。追踪由方法名、参数名和返回值自动生成 —— 这些信息本来就在
那里。

## 输出长什么样

运行你的代码,得到这样的执行追踪:

```
OrderService.placeOrder(customerId: "C-1234", productId: "SKU-MECHANICAL-KB", quantity: 2)
  CustomerService.findCustomer(customerId: "C-1234") -> Customer[id=C-1234, name=Alice Johnson, tier=GOLD]
  ProductCatalogService.lookupPrice(productId: "SKU-MECHANICAL-KB") -> 89.99
  InventoryService.reserve(productId: "SKU-MECHANICAL-KB", quantity: 2) -> Reservation[productId=SKU-MECHANICAL-KB, quantity=2]
  PaymentService.charge(customerId: "C-1234", amount: 179.98) -> PaymentConfirmation[transactionId=TXN-00001, amount=179.98]
-> OrderResult[orderId=ORD-00001, transactionId=TXN-00001, totalCharged=179.98, itemCount=2]
```

**出错的时候**,追踪让 bug 无处可藏:

```
OrderService.placeOrder(customerId: "C-BROKE", productId: "SKU-MOUSE-PAD", quantity: 3)
  CustomerService.findCustomer(customerId: "C-BROKE") -> Customer[id=C-BROKE, name=Charlie Broke, tier=STANDARD]
  ProductCatalogService.lookupPrice(productId: "SKU-MOUSE-PAD") -> 24.99
  InventoryService.reserve(productId: "SKU-MOUSE-PAD", quantity: 3) -> Reservation[productId=SKU-MOUSE-PAD, quantity=3]
  PaymentService.charge(customerId: "C-BROKE", amount: 74.97) !! PaymentDeclinedException: Payment declined for customer C-BROKE
!! PaymentDeclinedException: Payment declined for customer C-BROKE
```

`InventoryService.reserve` 被调用了,但整个追踪里找不到
`InventoryService.release`。bug 一目了然。

**追踪的质量取决于你的命名。** 同一段 Minecraft「玩家加入世界」流程,追踪两
次 —— 一次用领域命名,一次用泛化命名。

**领域命名:**

```
WorldServer.playerJoined(playerName: "Steve")
  WorldGenerator.generateChunk(x: 0, z: 0) -> Chunk(x: 0, z: 0, biome: "plains")
  PlayerInventory.addItem(item: OAK_LOG, quantity: 4) -> true
  CraftingTable.craft(recipe: WOODEN_PICKAXE) -> WOODEN_PICKAXE
  CreatureSpawner.spawnHostile(type: ZOMBIE, x: 10, y: 64, z: 20) -> Creature(type: ZOMBIE, ...)
```

**泛化命名:**

```
GameManager.handle(input: "Steve")
  DataProcessor.process(a: 0, b: 0) -> DataResult(a: 0, b: 0, tag: "plains")
  StateManager.update(type: 1, count: 4) -> true
  ThingFactory.create(type: 1) -> 1
  EntityHandler.execute(kind: 1, a: 10, b: 64, c: 20) -> Entity(kind: 1, ...)
```

相同的调用图,相同的返回值,只有命名不同。如果你的代码讲不出自己的故事,它就
需要重构 —— 这也正是 NarrativeTrace 还会[为你的命名打分](#第一条追踪之后)的
原因。

### 这对 AI 辅助开发意味着什么

每一行 `logger.info(...)` 都是 AI 编程工具——Claude Code、Copilot、Cursor——必须
解析、消耗 token 并绕开推理的一行。在典型的服务类里,日志占 30–50% 的行数。去掉
它们,相同的 token 预算就能覆盖更多真正的代码,模型看到的是代码做什么而不是它
如何记录自己做了什么,Pull Request 展示的也是业务逻辑变更,而不是逻辑与日志混杂
的变更。

还有后半段:每个测试还会输出一份 **AI 安全的结构化追踪**
(`structural/<类>/<场景>.nt`)—— 剥离了所有运行时值的调用结构,可以放心交给
AI 工具或提交到仓库。参见[结构化追踪格式](documentation/structural-trace-format.md)
(英文)。

### 与其他方案的对比

| 相比于 | NarrativeTrace |
|---|---|
| **结构化日志**(SLF4J + MDC)—— 日志语句要你自己写 | 从代码结构生成;请求追踪处于活动状态时,还会填充 `traceId` 这类关联字段,以及像 `bold elk soars` 这样可读的 `traceName`。它*使用* SLF4J,而不是取代它 |
| **分布式链路追踪**(OpenTelemetry、Jaeger)—— 跨服务的 Span,不含参数值 | 方法级调用树,带参数值和返回值。`narrativetrace-opentelemetry` 桥接两个世界:把 NarrativeTrace 树导出为带 `narrative.*` 属性的 OTel Span |
| **AOP 日志**(Spring AOP、AspectJ)—— 扁平而机械的进入/退出行 | 嵌套调用树,外加对产生它们的命名的清晰度评分 |

NarrativeTrace 不会取代你的生产告警或服务拓扑图。它提供的是两者都给不了的东
西:一份人类可读的执行叙事,同时兼作代码质量诊断。

### 不会取代你的日志框架

NarrativeTrace 不会碰你的日志框架。它不提供任何 appender、encoder 或
sink——你的 Logback 或 Log4j 配置、格式和输出目标,一如既往地继续工作。

它取代的是你手写的叙述——那些描述方法在做什么的
`log.info("Placing order {} for customer {}", ...)` 语句。被追踪方法会自动
生成这份叙事,取材于方法自身的签名和返回值。

这不是比喻。事件管道中持久、同步的那条路径,*就是*一个 SLF4J 监听器
(`narrativetrace-slf4j`)——生成的叙事通过与手写 `log.info(...)` 完全相同的
SLF4J 调用到达你的 appender。手写的日志语句照常在它身边工作:同一个
logger,同一条流,可以出现在被追踪方法之前、之中或之后。迁移过程中尽管自由
混用两者——完整示例见[与传统日志共存](documentation/zh-CN/配置指南.md#与传统日志共存)。

## 一条命令先试试

不用建项目,不用接线 —— 仓库自带一个演示启动器,运行示例应用并实时叙述(首次
运行会先构建示例):

```bash
./demo.sh --list                                  # ecommerce、clarity、minecraft、library
./demo.sh --example ecommerce --no-pause          # 旗舰级的 Spring 服务调用图
./demo.sh --example ecommerce --classic           # 同一次运行,以传统带时间戳的日志行呈现
./demo.sh --example minecraft --no-pause          # 上面那组命名对比的真实运行
./demo.sh --example ecommerce --lang zh-CN        # 同一次运行,经由领域术语表重新渲染
```

不加 `--no-pause` 时,演示会在每个场景后停下 —— `[Enter]` 继续,`q` 退出 ——
并且每个场景开头都会说明*这一条*追踪是如何接线的。各示例分别讲什么,见
[示例](narrativetrace-examples/自述文件.md)。

## 加到一个测试里

从「这个库挺有意思」到「我看到了自己代码的一份有用追踪」,最短的路径是 Gradle
插件加一个 JUnit 5 测试。需要 Java 17+
([完整兼容性矩阵](documentation/zh-CN/安装指南.md#兼容性))。

**1. 应用插件。** 它会添加依赖、设置 `-parameters` 编译器参数(缺少它追踪只能
显示 `arg0`、`arg1`)、配置测试 JVM,并提供 Jupiter 引擎:

```kotlin
// build.gradle.kts
plugins {
    id("ai.narrativetrace") version "0.2.0"
}
```

**2. 在一个测试里追踪一个服务:**

```java
@ExtendWith(NarrativeTraceExtension.class)
class OrderServiceTest {
    @Test
    void customerPlacesOrder(NarrativeContext context) {
        OrderService service = NarrativeTraceProxy.trace(
            new DefaultOrderService(), OrderService.class, context);

        service.placeOrder("C-1234", "SKU-KB", 2);
    }
}
```

**3. 运行测试套件:**

```bash
./gradlew test
```

**4. 打开叙事文件** —— 测试方法名成了场景名:

```text
build/narrativetrace/traces/OrderServiceTest/customer_places_order.md
```

套件里的每个测试都会写出自己的一组产物:

```text
build/narrativetrace/
├── traces/<测试类>/<场景>.md        面向人的叙事
├── traces/<测试类>/<场景>.json      同一条追踪的规范 JSON
├── diagrams/<测试类>/<场景>.mmd     Mermaid 时序图
├── structural/<测试类>/<场景>.nt    不含值的行为骨架(AI 安全)
└── clarity-report.md                整个套件的命名反馈
```

不用插件时,同样的配置是四行 Gradle 加两个依赖 ——
见[安装指南](documentation/zh-CN/安装指南.md)。

想继续往下走 —— 重命名方法看清晰度评分下降、加上 `@NotTraced` 看一个值被脱
敏、打开审批模式看一个 `.received.nt`?→ [First 10
Minutes](documentation/first-10-minutes.md)(英文)用真实的、实际运行出来的
输出把这一切都走一遍。

### Gradle 还是 Maven?

运行时 jar 就是普通的 Maven 制品。`ai.narrativetrace:narrativetrace-core:0.2.0`
以及它旁边的每一个模块,在 Maven 构建中同样可以解析、同样可以工作;库本身没有
任何东西是 Gradle 专有的。真正属于 Gradle 的是上面那个插件 —— 它只是替你接好
编译器参数、依赖和测试 JVM 的便利工具。

也就是说:同样的 jar,两种构建工具都能用 —— 但今天一流的、有文档的配置体验是
Gradle。一个完整的 Maven 示例现在已经兑现了这个承诺:
[`narrativetrace-maven-example`](narrativetrace-maven-example) 是一个独立的
`pom.xml`,消费本地安装好的制品,Surefire 接好了输出目录,JUnit 5 扩展按
Maven 的方式注册 —— [Maven 指南](documentation/maven-guide.md)(英文)从头到
尾讲解了整个过程,包括真正重要的那一点差异:没有插件,也就没有自动的编译器
参数,也没有 `approveNarratives` 任务,以及每一项该怎么替代。目前还没有任何
制品发布到 Maven Central,所以两者都从 `./gradlew publishToMavenLocal` 开
始。

## 选择你的集成方式

大多数人从测试开始。接下来去哪里:

| 你想要 | 从这里开始 |
|---|---|
| 在测试中获得追踪,接线最少 | Gradle 插件 + `narrativetrace-junit5` |
| 同样的效果,但用 JUnit 4 | `narrativetrace-junit4` |
| 在纯 Java 中精确决定包装哪些对象 | `narrativetrace-proxy`(JDK 动态代理) |
| 自动追踪 Spring Bean | `narrativetrace-spring` |
| 生产环境中 Spring 的 HTTP 请求生命周期 | `narrativetrace-spring-web`(装配 `narrativetrace-servlet`) |
| 任意 Servlet 应用,不用 Spring | `narrativetrace-servlet` |
| Micronaut 的 Bean 与请求 | `narrativetrace-micronaut` + `narrativetrace-micronaut-http` |
| **零代码改动** —— 一个你无法或不愿修改的应用 | `narrativetrace-agent`(Java Agent) |
| 跨线程的异步可见性(`@Async`、Reactor、线程池) | `narrativetrace-micrometer`,或手动使用 `ContextSnapshot` |
| 把追踪送进生产日志流 | `narrativetrace-slf4j` |
| OpenTelemetry Span | `narrativetrace-opentelemetry` |

**零代码改动**值得展开说,因为它连构建脚本都不用动 —— Java Agent 在类加载时
改写它们:

```bash
java -javaagent:narrativetrace-agent.jar=packages=com.example.myapp.* -jar your-app.jar
```

指定包下每个类的每个非私有方法,方法体内都会被编译进进入/退出捕获;这些包之外
的一切都不会被触碰。包匹配只做包含匹配,并且按分隔符边界比较,因此 `com.acme`
永远不会匹配到 `com.acmeExtra`。在没有可达 classpath 的应用服务器上,请使用
`-standalone` jar 和 `loggingJars=`;Agent 刻意不自带任何 SLF4J provider。上面
每一条路径的具体配方见[安装指南](documentation/zh-CN/安装指南.md);选择时用的
决策图见 [Choosing an Integration](documentation/choosing-an-integration.md)
(英文)。

## 模块

| 模块 | 适用场景 |
|------|----------|
| `narrativetrace-api` | 仅编译期的契约:注解、事件模型与各 SPI。通常它会随 `core` 传递依赖进来 —— 只有当你是面向该契约发布的库作者时,才单独依赖它。 |
| `narrativetrace-core` | 始终必需。运行时上下文、管道、渲染器、配置与导出。零运行时依赖。 |
| `narrativetrace-proxy` | 使用 JDK 动态代理追踪(最常见)。 |
| `narrativetrace-junit5` | 在 JUnit 5 测试中自动追踪。 |
| `narrativetrace-junit4` | 在 JUnit 4 测试中自动追踪。 |
| `narrativetrace-spring` | 自动包装 Spring Bean。 |
| `narrativetrace-micronaut` | 自动包装 Micronaut Bean。 |
| `narrativetrace-slf4j` | 将追踪经由 SLF4J/Logback 输出。 |
| `narrativetrace-clarity` | 分析方法/参数的命名质量。 |
| `narrativetrace-glossary` | 从追踪中采集领域术语表(通用语言)。 |
| `narrativetrace-diagrams` | 生成 Mermaid/PlantUML 时序图。 |
| `narrativetrace-opentelemetry` | 将追踪树导出为 OpenTelemetry Span,或通过装饰器实时创建 Span。 |
| `narrativetrace-micrometer` | 通过 Micrometer context-propagation 实现跨线程追踪传播。 |
| `narrativetrace-agent` | 字节码级追踪,无需接线代理。 |
| `narrativetrace-servlet` | 任意 Servlet 应用中的生产请求生命周期(不要求 Spring)。 |
| `narrativetrace-spring-web` | 用于 `narrativetrace-servlet` 的 Spring `@Configuration`——通过 `ObjectProvider` 自动装配过滤器。 |
| `narrativetrace-micronaut-http` | Micronaut 响应式 HTTP 过滤器,按请求管理追踪生命周期。 |
| `narrativetrace-examples` | 参考应用(仅源码):电商、Minecraft 命名对比、图书借阅、酒店预订清晰度演示、由 Java Agent 零代码追踪的 WildFly 上 EJB 4(Jakarta EE)保险理赔 WAR(`ejb4`,基于 Docker 的测试),以及 `common` 中的共享示例工具。 |

**典型起步组合:**Gradle 插件 —— 它会替你装上 `core` + `proxy` + `junit5`。

## 第一条追踪之后

追踪跑起来之后,测试套件还会给你三样东西,每一样背后都有一份指南:

**清晰度评分。** 如果追踪*就是*代码,那么追踪的质量*就是*代码的质量。
`clarity-report.md` 为运行过的每个方法名、类名和参数名打分:像 `processData`、
`handleRequest` 这样的泛化命名得分低,像 `reserveInventory`、`customerId` 这样
的领域专属命名得分高,而 `clarityCheck` 可以按你设定的阈值让构建失败。目前仍
处于实验阶段。
→ [清晰度指南](documentation/zh-CN/清晰度指南.md)

**叙事审批测试。** 每次运行都会把每个场景的*结构*与最近一次绿色运行比较,并在
套件结束时给出一行:

```
NarrativeTrace — Suite complete
  5 scenarios recorded
  Clarity: 100% high | 0% moderate | 0% low
  Reports: build/narrativetrace
  Since last green: 4 scenarios unchanged · 1 changed: "Customer places order" (+1 call InventoryService.release)
```

打开审批模式(`narrativetrace.approval=true`,或插件 DSL 中的
`approval.set(true)`),这个结构就变成一份已提交的契约:通过的测试若其骨架与
`src/test/narratives/<类>/<场景>.approved.nt` 基线不同,将以可读的 diff 失败,
新骨架作为 `.received.nt` 落在基线旁,`./gradlew approveNarratives` 提升你已审
阅的文件。行为变更 —— 包括 AI 代理在重构中悄悄带入的变更 —— 必须经过审阅和批
准,而不是仅仅能编译通过。基线不含值,因此在多次运行之间保持稳定,可以放心提
交。
→ [结构化追踪格式](documentation/structural-trace-format.md)(英文)

**并发。** fork-join(分叉汇合)并行(`ForkGroup`)、即发即弃任务
(`FireAndForgetGroup`)以及 `CompletableFuture` 返回值都是追踪树中的一等公民,
渲染时带 `⑂ fork` / `⑃ join` 标记、线程名和等待时间分析。`captureTrace()` 刻意
限定在线程作用域内 —— 请在记录的那个线程上捕获,或用 `ContextSnapshot.wrap()`
把工作嫁接进父追踪。
→ [完整参考:并发](documentation/llms-full.md#concurrency)(英文)

## 隐私与安全

这个库运行在你的进程内,并写出你的团队会分享的文件。这意味着什么,一屏说完:

| 保证 | 如何成立 |
|---|---|
| **脱敏是无条件的** | `@NotTraced` 与基于名称的拒绝列表(`password`、`token`、`cvv`、`ssn` 等)适用于所有已发布的输出路径 —— 测试追踪、CI 产物、容器日志、Agent 叙述。没有任何阶段、开关或属性能关掉它们(所有者决定,2026-08-16)。它能穿透一层容器(`Optional`、`Future`、`AtomicReference`、`Map.Entry`);`@NotTraced` 的优先级高于自定义 `toString()`,而指名了被脱敏成员的 `{param.path}` 模板会解析为 `[REDACTED]`。 |
| **AI 安全产物根本不含值** | 结构化 `.nt` 文件只包含名称、层级和结果类型。没有什么可脱敏,提示注入面为零 —— 而且这是一条属性测试,不是一句政策。 |
| **追踪失败不会拖垮你的应用** | 记录在所有路径上都做了异常隔离,管道的两个消费者也会吞掉自己的错误。抛异常的 `toString()`、写满的缓冲区或损坏的 appender,都不会改变你的方法返回或抛出什么。 |
| **资源占用有上界** | 带缓冲的分析路径是一个固定大小的环形缓冲区(默认 65,536 个槽位,由 `narrativetrace.buffer.capacity` 设定),它选择丢弃而不是阻塞 —— 而且会明说:丢失过事件的捕获会在自己的页脚打印丢弃计数。值渲染在字符串长度、集合大小、对象宽度和嵌套深度上都有上限。 |
| **输出无法被伪造** | 渲染出的值都会转义,不能伪造日志行、破坏 Markdown,也不能破坏图表语法。 |
| **与其他包装器叠加是安全的** | AOP 代理、契约库、容器拦截器和其他 Agent 都可能包装 NarrativeTrace 也在包装的同一个方法。嵌套顺序会改变追踪*读起来*的样子——但绝不会改变它*返回或抛出*的内容:记录在所有路径上都做了异常隔离,绝不会替换结果或异常。目标是每次业务边界穿越对应一个追踪帧;机制性代码(桥接方法、容器视图桩、其他库生成的代码)不是叙述的对象。 |

两条诚实的限制。第一,值唯一可能绕过脱敏的途径,是应用代码自己用
`RedactionPolicy.DISABLED` 构造 `ValueRenderer` —— 那是你自己源码中一次刻意
的、可评审的行为,而不是一种配置状态。第二,捕获通过反射读取*字段*,从不调用你
的 getter,但它确实会调用自定义 `toString()`、`@NarrativeSummary` 方法、record
组件访问器,以及 `@Narrated`/`@OnError` 模板中指名的属性路径;请像对待调试器一
样保持它们纯净。目前的范围控制只有包含匹配 —— Agent 用 `packages=`,Spring 和
Micronaut 用基础包 —— 还没有排除列表。

→ 逐行核对代码后得到的脱敏契约见 [Privacy and
Redaction](documentation/privacy-and-redaction.md)(英文),各阶段的隐私姿态
见[生命周期指南](documentation/zh-CN/生命周期指南.md),完整
的纯净性契约见[注解指南](documentation/zh-CN/注解指南.md),NarrativeTrace 与
AOP 代理、契约库和其他 Agent 叠加时按机制划分的详情,见
[选择集成方式 § 与其他包装器叠加](documentation/zh-CN/选择集成方式.md#与其他包装器叠加)。

## 性能

我们在热路径上下了真功夫,但不会宣称「零开销」—— 追踪要做事,做事就有代价。我们
的实测数据(JMH、JDK 17、`-prof gc`,一次基准操作 = 一次调用):

- **追踪关闭或上下文未激活:** JDK 动态代理相比直接调用(在这套基准环境中约
  9–12 ns)每次调用增加约 12–26 ns 和 24 B/op —— 参数用的一个 `Object[]`。
  `isActive()` 这道门在追踪关闭时跳过全部捕获、渲染和反射工作。
- **字节码 Agent 的未激活路径不产生任何分配。** `agent_OFF` 实测 **56 B/op** ——
  与直接调用相同 —— 每次操作约 37–44 ns,其中还包含基准自身在测量区间内做的上
  下文切换。被插桩的方法在整理任何数据之前先读一个静态的 `isActive()`:追踪关闭
  时,不装箱任何参数,不构建任何数组,不发起任何调用。
- **追踪激活:** 一次带参数捕获与值渲染的代理调用实测 **0.6–1.7 µs**,分配
  **约 1.0–1.5 kB/op**,具体取决于注解。捕获一棵单节点追踪并重置上下文的开销为
  1.3–2.3 µs 和 2.8–3.1 kB。

这些数字来自在共享容器上运行的 [JMH 基准](narrativetrace-benchmarks/):分配数
据可逐字节复现,纳秒数据则随机器负载浮动 —— 因此两者都作为*上限*记录在
[`baseline.txt`](narrativetrace-benchmarks/baseline.txt) 和
[`allocation-baseline.txt`](narrativetrace-benchmarks/allocation-baseline.txt)
中,让回归在提交之间始终可见。它们高于本 README 在 2026-08-31 之前给出的数字,
那些数字测的是一个做得更少的管道:现在每次进入和退出都会经环形缓冲区把事件发布
到保留存储,每个 Span 都携带 W3C 追踪身份。性能是持续关注的事项,不是已经解决的
问题。对于极热的循环,请使用 `TracingLevel.OFF` 或收窄追踪范围。

## 哪些免费,哪些是 Pro

**免费**的是本仓库中的一切 —— 以 BSL 1.1 源码可得,可免费用于生产,并在每个版
本发布四年后转为 Apache 2.0:完整运行时、各种格式的按测试追踪、带 delta 报告与
审批模式的结构化 `.nt` 产物、清晰度评分、领域术语表与翻译追踪视图,以及上表中的
每一种集成。契约 jar `narrativetrace-api` 直接就是 Apache 2.0。

**Pro** 是*跨*运行、跨仓库的智能能力:聚合流程摘要与运行时依赖图、迁移 diff 与
语义 diff、聚合时序图、PR 评审机器人与漂移分析、清晰度的历史趋势、面向 AI 代理
的 MCP 工具与服务器,以及审计与合规套件(`@AuditEvent`、策略引擎、字段掩码、控
制项可追溯性)。其中并非所有能力都已发布。[功能指南](documentation/zh-CN/功能指南.md)
才是权威的状态表:它为每一项功能标注 Open、免费、Pro、开发中或已规划,并为每一
条已发布的行引用背后的代码。

## 文档

**[documentation/](documentation/自述文件.md)** 索引了本仓库中的每一份文档 ——
英文指南及其西班牙文、中文译本。完整的已发布文档 —— 包括本仓库中没有对应文件的
页面 —— 位于 **[narrativetrace.ai/zh/docs](https://narrativetrace.ai/zh/docs.html)**。
面向 AI 代理:[`documentation/llms.txt`](documentation/llms.txt) 是机器可读索引,
[`documentation/llms-full.md`](documentation/llms-full.md) 是单文件完整参考,
每个已发布模块都附带 Javadoc 丰富的源码 jar
([javadoc.io](https://javadoc.io/doc/ai.narrativetrace))。

从这里开始:

- [First 10 Minutes](documentation/first-10-minutes.md)(英文) —— 一个极简服务、一个 JUnit 测试、八个步骤得到一条真实追踪,全部是实际运行出来的输出
- [安装指南](documentation/zh-CN/安装指南.md) —— 依赖、全部集成方式、捕获的工作原理、追踪输出设置
- [Choosing an Integration](documentation/choosing-an-integration.md)(英文) —— 你到底需要哪个模块,以决策图呈现
- [配置指南](documentation/zh-CN/配置指南.md) —— 追踪级别,JUnit/Gradle/Spring/Micronaut/SLF4J 配置
- [Gradle插件指南](documentation/zh-CN/Gradle插件指南.md) —— DSL 参考、质量门禁、常用配方
- [注解指南](documentation/zh-CN/注解指南.md) —— `@Narrated`、`@OnError`、`@NotTraced`、`@NarrativeSummary`、`@EnableNarrativeTrace`

深入了解:

- [生命周期指南](documentation/zh-CN/生命周期指南.md) —— NarrativeTrace 在流程中的位置:开发、CI/验收、生产
- [Privacy and Redaction](documentation/privacy-and-redaction.md)(英文) —— 逐行核对代码后得到的脱敏契约
- [What to Commit](documentation/what-to-commit.md)(英文) —— 哪些生成文件是 CI 产物,哪些是需要提交的已审阅基线
- [Troubleshooting](documentation/troubleshooting.md)(英文) —— 常见故障的症状 → 原因 → 解决办法
- [Spring 集成指南](documentation/zh-CN/Spring集成指南.md) —— Bean 追踪、Servlet 过滤器、`@Async` 传播
- [Micronaut 集成指南](documentation/zh-CN/Micronaut集成指南.md) —— Bean 追踪、HTTP 过滤器、配置属性
- [清晰度指南](documentation/zh-CN/清晰度指南.md) —— 评分模型、NLP 组件、JUnit 集成
- [功能指南](documentation/zh-CN/功能指南.md) —— 覆盖所有平台的每项功能的规范目录,含层级与状态
- [结构化追踪格式](documentation/structural-trace-format.md) —— 支撑 delta 报告与审批测试的无值 `.nt` 产物(英文)
- [完整参考](documentation/llms-full.md) —— API、捕获内部机制、配置、集成配方与故障排查,汇于一个文件(英文)
- [领域术语表](https://narrativetrace.ai/doc.html?p=docs/glossary.md) —— 从追踪中采集的通用语言术语表,以及翻译后的追踪视图(英文)

## 从源码构建

```bash
./gradlew test                                     # 运行所有测试
./gradlew check                                    # 测试 + PMD + JaCoCo 覆盖率 + 其余门禁
./gradlew :narrativetrace-examples:runExamples     # 依次运行全部示例
./gradlew :narrativetrace-examples:traceExamples   # 运行测试 → 生成 Markdown 追踪文件
./gradlew :narrativetrace-examples:ejb4:dockerTest # 仅由 Agent 追踪的 WildFly 上 EJB 4 WAR(需要 Docker)
```

## 许可证

NarrativeTrace 的 API 与输出格式是开放标准(Apache 2.0)。其运行时免费且源码可得
(BSL 1.1,每个版本发布四年后转为 Apache 2.0),因为在你的进程中运行的代码应当可以
审计。其智能能力是商业的。

| 制品 | 许可证 | 含义 |
|---|---|---|
| `narrativetrace-api` | [Apache 2.0](LICENSE-APACHE) | 注解、事件模型与各 SPI——你的代码所编译依赖的一切。作为开放标准,任何实现都可以对齐它。 |
| 其余全部 `ai.narrativetrace` 制品 | [BSL 1.1](LICENSE) | 运行时。可免费用于生产,也包括你提供给自己客户的产品与服务。唯一的排除项是把 NarrativeTrace 本身——或价值实质上源自它的产品或服务——作为日志、追踪或代码叙事产品或服务提供给第三方。每个发布版本在发布四年后转为 Apache 2.0。 |
| 文档（散文指南） | [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/) | 各指南与参考文档。输出格式规范与 JSON 模式为 Apache 2.0——它们是开放标准。 |

各模块属于哪个类别声明在 [`licensing.properties`](licensing.properties) 中,
并且构建会拒绝许可证无法支撑的依赖关系图。

运行时**不是**开源,本 README 也不会这样称呼它。它是免费且源码可得的,并带有一个
有明确日期的转为开源的承诺。

<!-- legal:trademark:begin -->
两份许可证都不授予任何商标权:NarrativeTrace 是 Empower Agile 的商标,获得使用、
复制或修改代码的许可,并不等于获得在你自己的发行版或服务中使用该名称的许可。
<!-- legal:trademark:end -->

### 用大白话说的许可证

`narrativetrace-api` 的 jar 包、输出格式规范以及 JSON 模式均为 Apache 2.0——彻底开源,
除 Apache 自身的限制外不附加任何限制。

<!-- legal:plain-words:begin -->
**免费运行。** 运行时依据 Business Source License 1.1 源码可得:你可以阅读它、审计它、修改它,
并可免费用于生产环境——包括用在你提供给自己客户的产品与服务中。

**唯一的排除项。** 你不得将 NarrativeTrace 本身——或价值实质上源自它的产品或服务——作为日志、
追踪或代码叙事产品或服务提供给第三方。

**它会在某个日期开放。** 每个发布版本都会在发布四年后转为 Apache 2.0;
确切日期印在该版本的 LICENSE 中。

*这份摘要只是一种礼貌说明,不是许可证本身。LICENSE 文件才是唯一具有约束力的文本;
若二者不一致,以 LICENSE 为准。*
<!-- legal:plain-words:end -->
