# Pinned security-scanner versions — bump here, nowhere else. scripts/install-scanners.sh reads
# this file; CI installs the scanners through this script and carries no scanner version, flag or
# threshold of its own (thin-CI rule — documentation/security-tooling.md names the update
# procedure).
SEMGREP_VERSION=1.177.0
OSV_SCANNER_VERSION=v2.6.0
