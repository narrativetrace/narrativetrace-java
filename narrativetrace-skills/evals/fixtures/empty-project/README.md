# empty-project

A cold-install starting point for Tier B's `add-narrative-tracing` happy-path case: nothing
installed yet, no NarrativeTrace dependency, no wrapped class. Scaffolded into a scratch copy for
each trial; never built or run by this repository's own Gradle (it is not `include()`d in
`settings.gradle.kts`).
