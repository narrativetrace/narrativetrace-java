# SPDX-License-Identifier: BUSL-1.1
# Licensed under the Business Source License 1.1 (see LICENSE); Change Date: four years from publication; Change License: Apache-2.0
# Copyright (c) 2026 Empower Agile
# Duplication baseline (PMD CPD, 60-token floor, identifiers/literals ignored) — main sources
# only; the test tree is reported by `duplicationReport` but never gates (see
# documentation/duplication.md). `duplicationCheck` fails when the measured main percentage rises
# more than 0.3 points above `main.percent`, or when a non-exempt cluster is larger than
# `main.largestCluster` — see config/duplication/exemptions.txt for what is exempt and why.
#
# Floored to measured (the same idiom this build already uses for coverage and mutation-score
# floors): lower this with the commit that removes the duplication, never raise it without a
# reasoned exemption line instead.
# main.percent still counts exempt data-table rows (the clarity dictionaries, TraceNamer's word
# tables, GenericTokenDetector, and the CanonicalEntry/SpanContext builders, all exempted by path
# — see config/duplication/exemptions.txt). main.largestCluster is the largest NON-exempt cluster.
# Lowered from 21.4/203 by the resolver run rebased onto the exemptions ruling: five clusters of
# shared logic extracted across the core, diagrams and clarity modules (the tree-renderer carry
# buffer, the diagram quoter/participant walk, the JSON exporter's array writer, the Markdown
# renderer's thrown-outcome detail, and the abbreviation-averaging move into the dictionary). The
# 203-token sibling-carry cluster is gone; the largest remaining non-exempt cluster is now 168
# tokens (CanonicalEntrySerializer's repeated field-write shape).
#
# Lowered again from 20.6/168 to measured 19.4/168 (owner ruling 2026-09-13, same day): the
# resolver-flagged JsonEscape/DoctorRender pair (84 tokens, an escape-on-write clone across the
# narrativetrace-cli zero-dependency boundary — see config/duplication/exemptions.txt) is ruled a
# ledgered exemption rather than folded, since narrativetrace-cli takes zero runtime dependencies by
# contract and has nothing to fold into. The exemption itself does not change the measured
# percentage (main.percent still counts exempt clusters, per the header above); the 20.6 -> 19.4
# drop is unrelated new source volume landing between the two recordings diluting the same
# duplicated-token count. largestCluster is unchanged: the exemption does not touch the 168-token
# CanonicalEntrySerializer cluster, still the largest non-exempt one.
recorded=2026-09-13
commit=2026-09-13-jsonescape-doctorrender-exemption
main.percent=19.4
main.largestCluster=168
